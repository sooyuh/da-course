# Python数据分析训练项目合集（10个项目）

## 项目一：电商销售数据清洗与质量诊断

### 1. 项目背景
某电商平台提供了包含12万条记录的销售数据CSV文件，但数据存在缺失值、重复记录、格式不一致、异常值等多种质量问题，严重影响后续分析。作为数据分析师，你需要对原始数据进行全面的清洗与质量诊断，输出一份数据质量报告。

### 2. 学习目标
- 掌握pandas读取CSV文件、查看数据概况的方法
- 学会识别和处理缺失值（`isnull()`、`fillna()`、`dropna()`）
- 掌握重复值检测与去重（`duplicated()`、`drop_duplicates()`）
- 学会数据类型转换与格式统一（`astype()`、`pd.to_datetime()`）
- 掌握异常值识别方法（箱线图、分位数法）

### 3. 核心任务
1. 使用`pd.read_csv()`加载数据，通过`info()`和`describe()`进行初步诊断
2. 统计各列缺失值数量，根据业务规则对数值列用均值/中位数填充，对类别列用众数填充
3. 基于订单号检测并删除重复记录
4. 将日期列转换为datetime类型，提取年/月/日/星期信息
5. 使用箱线图识别销售额中的异常值（如大于3倍标准差的数据），结合业务规则判断是否剔除
6. 输出一份“数据清洗报告”，包含清洗前后的数据质量对比表

### 4. 数据说明
- **数据源**：可下载Kaggle上的“E-commerce Sales Dataset”或使用老师提供的模拟数据`sales_raw.csv`
- **关键字段**：订单号、用户ID、商品ID、商品品类、销售数量、单价、销售金额、订单日期、用户年龄、用户性别等

### 5. 技术要点
- `pd.read_csv()`, `df.info()`, `df.describe()`
- `isnull()`, `sum()`, `fillna()`, `dropna()`
- `duplicated()`, `drop_duplicates()`
- `astype()`, `pd.to_datetime()`, `dt.year`等
- 箱线图：`sns.boxplot()` 或 `df.boxplot()`
- 条件筛选与赋值

### 6. 任务步骤（参考）
1. 加载数据，查看前5行
2. 检查每列缺失比例，绘制缺失值热力图
3. 对数值列缺失值：用该列中位数填充；对分类列缺失值：用众数填充
4. 检查订单号是否有重复，删除重复订单
5. 将订单日期转为datetime，新增“星期几”列
6. 计算销售额的Z-score，标记异常值，按业务规则（如销售额<=0或>10000）进行过滤
7. 生成清洗前后的对比统计表

### 7. 拓展思考
- 如果某商品单价异常高但销售数量很少，应如何处理？
- 数据清洗的步骤顺序对结果有什么影响？
- 如何用编程自动生成数据质量报告（含表格和图表）？

---

## 项目二：用户行为日志分析与漏斗转化

### 1. 项目背景
电商平台记录了用户在双十一活动期间的行为日志，包含用户ID、商品ID、行为类型（点击/收藏/加购/支付）、时间戳等信息。运营团队希望了解用户从浏览到最终支付的全链路转化情况，找出流失最严重的环节，并计算PV、UV、付费率等核心指标。

### 2. 学习目标
- 掌握时间戳数据的解析与特征提取
- 学会按用户聚合统计行为数据
- 掌握漏斗分析的实现方法
- 学会计算PV、UV、付费率、复购率等核心指标
- 掌握基础数据可视化（matplotlib/seaborn）

### 3. 核心任务
1. 加载用户行为日志数据，将时间戳解析为日期和小时，计算每日/每小时的PV和UV
2. 按用户统计行为序列，分析“点击→收藏/加购→支付”各环节的转化率和流失率，绘制漏斗图
3. 计算整体付费率（付费用户数/活跃用户总数）和复购率（购买两次及以上的用户数/总用户数）
4. 识别“只加购不支付”的用户群体，分析其行为特征（如加购时间、商品类目分布）
5. 输出转化率优化建议

### 4. 数据说明
- **数据源**：淘宝双十一用户行为公开数据集（User Behavior Data from Taobao），或老师提供的`user_behavior.csv`
- **关键字段**：user_id, item_id, behavior_type（1=点击,2=收藏,3=加购,4=支付）, timestamp

### 5. 技术要点
- `pd.to_datetime()` 解析时间戳
- `groupby()` 聚合计数（PV/UV）
- `pivot_table()` 构建用户行为漏斗矩阵
- 漏斗图绘制：可使用`plotly`或手动计算比例后用`matplotlib`绘制条形图
- 条件筛选：`df[df.behavior_type==4]`

### 6. 任务步骤（参考）
1. 加载数据，将timestamp转为datetime，提取日期和小时
2. 计算每小时PV和UV，绘制折线图
3. 统计每个用户的行为序列：如果用户有点击、收藏、加购、支付中的任意行为，按时间排序，判断其转化路径
4. 计算各环节转化率：点击→收藏、收藏→加购、加购→支付，绘制漏斗图
5. 计算付费率 = 有支付行为的用户数 / 总用户数；复购率 = 购买次数≥2的用户数 / 总支付用户数
6. 筛选出“加购但从未支付”的用户，分析其加购时间分布和商品品类偏好

### 7. 拓展思考
- 如果用户跨天完成购买，漏斗分析应该如何处理？
- 如何计算不同渠道（如PC端/移动端）的转化率差异？
- 漏斗分析中，如何定义“有效会话”？

---

## 项目三：商品销售趋势分析与库存预警

### 1. 项目背景
电商运营团队需要掌握各品类商品的销售趋势，识别畅销品与滞销品，建立库存预警机制，为采购和促销策略提供数据支持。

### 2. 学习目标
- 掌握按时间维度的聚合分析（日/周/月）
- 学会计算移动平均和环比增长率
- 掌握TopN分析与分组排名
- 学会使用pandas进行数据透视与多维度分组统计

### 3. 核心任务
1. 按日期聚合每日销售额，绘制销售趋势折线图，计算7日移动平均平滑趋势
2. 按商品品类分组，计算各品类月度销售额环比增长率，识别增长最快和下滑最严重的品类
3. 找出销售额Top10的商品，以及滞销（连续30天无销售）的商品列表
4. 建立简单的库存预警规则：当某商品库存量低于过去30天日均销量的3倍时，标记为“需补货”
5. 输出一份商品运营报告，包含畅销榜、滞销榜和补货建议清单

### 4. 数据说明
- **数据源**：基于项目一清洗后的销售数据，并增加“库存量”字段（可模拟生成）
- **关键字段**：商品ID、商品品类、销售日期、销售数量、销售额、当前库存

### 5. 技术要点
- `groupby()` + `resample()` 按日期重采样
- `rolling(window=7).mean()` 移动平均
- `pct_change()` 计算环比增长率
- `nlargest()` / `nsmallest()` 获取TopN
- 条件判断与标记

### 6. 任务步骤（参考）
1. 按日期聚合销售额，绘制每日销售额折线图，并叠加7日移动平均线
2. 按月聚合品类销售额，计算各品类环比增长率，筛选出增长最快/下降最多的前3名品类
3. 计算每个商品的总销售额，找出Top10商品；计算每个商品最近一次销售日期，筛选出距今≥30天无销售的商品作为滞销品
4. 对每个商品，计算过去30天的日均销量（排除滞销期），与当前库存比较，若库存 < 日均销量*3，则标记为“需补货”
5. 生成Excel报告，包含多个sheet

### 7. 拓展思考
- 节假日效应如何影响销售趋势？如何剔除周期性因素？
- 库存预警阈值（3倍）是否合理？如何通过历史数据优化阈值？
- 如何将补货建议与采购成本优化结合？

---

## 项目四：购物篮分析与关联规则挖掘

### 1. 项目背景
运营团队发现某些商品经常被一起购买，希望通过数据分析发现商品之间的关联规律，优化捆绑销售策略和商品货架布局。这是电商数据分析中最经典的购物篮分析（Market Basket Analysis）场景。

### 2. 学习目标
- 掌握将订单明细转换为订单-商品交易矩阵的方法
- 理解支持度（Support）、置信度（Confidence）、提升度（Lift）的含义与计算
- 学会使用mlxtend库实现Apriori算法进行关联规则挖掘
- 掌握关联规则的可视化与业务解读

### 3. 核心任务
1. 从订单明细数据中提取订单ID和商品ID，去重后构建订单-商品交易矩阵（每个订单一行，每个商品一列，值为0/1表示是否购买）
2. 使用`apriori()`函数设置`min_support`阈值，挖掘频繁项集
3. 使用`association_rules()`生成关联规则，按提升度（Lift）降序排列，筛选Lift>1且有意义的规则
4. 分析挖掘出的强关联规则（如“购买A的用户有70%会同时购买B”），给出捆绑销售建议
5. 绘制支持度-置信度散点图，可视化规则分布
6. 输出购物篮分析报告，包含Top10关联规则及营销策略建议

### 4. 数据说明
- **数据源**：Kaggle上的“Online Retail Dataset”（在线零售数据集），或老师提供的`online_retail.csv`
- **关键字段**：InvoiceNo（订单号）、StockCode（商品代码）、Description（商品描述）、Quantity（数量）、InvoiceDate（日期）

### 5. 技术要点
- `groupby()` + `unstack()` 构建交易矩阵
- `mlxtend.frequent_patterns.apriori()`
- `mlxtend.frequent_patterns.association_rules()`
- 支持度、置信度、提升度的业务含义
- 散点图：`plt.scatter()`

### 6. 任务步骤（参考）
1. 数据预处理：删除退货订单（InvoiceNo含C的），只保留正常交易
2. 每个订单内商品去重（一个订单多次购买同一商品视为一次），生成订单-商品列表
3. 使用pandas的`crosstab()`或`pivot_table()`生成交易矩阵（one-hot编码）
4. 调用`apriori()`，设置`min_support=0.01`（根据数据稀疏程度调整）
5. 调用`association_rules()`，按`lift`降序，筛选`lift > 1`且`confidence > 0.5`的规则
6. 解读Top10规则：例如“{全脂牛奶} → {酸奶}”，提升度2.5表示购买全脂牛奶的用户购买酸奶的概率是普通用户的2.5倍
7. 绘制支持度-置信度散点图，用颜色表示提升度

### 7. 拓展思考
- 如何处理大量商品导致的矩阵稀疏问题？
- 关联规则挖掘结果中可能出现冗余规则（如A→B和A,B→C），如何精简？
- 如何将关联规则与个性化推荐系统结合？

---

## 项目五：RFM用户价值分层与精细化运营

### 1. 项目背景
电商平台的用户量已达数十万，但不同用户的价值差异巨大。运营团队希望建立一套用户价值分层体系，识别高价值用户、潜力用户和流失风险用户，实现差异化运营。

### 2. 学习目标
- 掌握RFM模型（最近购买时间R、购买频率F、消费金额M）的构建方法
- 学会对连续数值进行离散化分箱（`pd.cut()`、`qcut()`）
- 掌握用户分层打分与分群逻辑
- 学会分析不同用户群体的消费行为差异

### 3. 核心任务
1. 基于订单数据，按用户计算三个RFM指标：
   - R：最近一次购买距离当前日期的天数
   - F：购买总次数
   - M：消费总金额
2. 对R、F、M分别进行分箱打分（如1-5分，分数越高代表表现越好）
3. 计算每个用户的RFM综合得分或得分组合（如“R5F5M5”），将用户划分为：高价值用户、重点保持用户、重点挽留用户、一般用户、流失用户
4. 分析各层级用户的人数占比、消费贡献占比、平均客单价等特征
5. 针对不同用户群体输出运营策略建议

### 4. 数据说明
- **数据源**：项目一清洗后的销售数据（含订单日期、用户ID、订单金额）
- **关键字段**：user_id, order_date, amount

### 5. 技术要点
- `groupby().agg()` 聚合计算R/F/M
- `pd.to_datetime()` 计算日期差
- `pd.cut()` / `qcut()` 分箱打分
- 用户分层标签生成
- 分组统计与可视化

### 6. 任务步骤（参考）
1. 设定分析时间点（如数据中最大日期+1天），计算每个用户的R、F、M
2. 分别对R、F、M进行分箱：R值越小越好（近期购买），可逆向打分；F和M越大越好，正向打分。使用`qcut`分为5等份，赋予1-5分
3. 构建RFM总分 = R_score + F_score + M_score（或加权），或保留三位组合
4. 根据业务经验定义分层规则：
   - 高价值用户：R>=4且F>=4且M>=4
   - 重点保持用户：R<=2但F>=4且M>=4（最近没来但历史价值高）
   - 重点挽留用户：R<=2且F<=2但M>=4（即将流失的高消费用户）
   - 一般用户：其他
   - 流失用户：R=1且F=1且M=1
5. 统计各层人数、总消费金额、客单价、平均购买次数
6. 输出策略：高价值用户→专属客服；重点挽留用户→发送优惠券；流失用户→召回活动

### 7. 拓展思考
- RFM分箱用等频分箱还是等宽分箱？各有什么优缺点？
- 如何引入货币的时间价值（例如近期的消费权重更高）？
- 除了RFM，还可以加入哪些行为指标（如浏览深度、优惠券使用率）？

---

## 项目六：用户留存分析与复购行为预测

### 1. 项目背景
电商平台发现新用户的复购率持续偏低，影响了平台的长期增长。数据分析团队需要深入了解用户的复购行为模式，识别影响复购的关键因素，并为运营团队提供改善留存的建议。

### 2. 学习目标
- 掌握留存率的计算方法和留存曲线绘制
- 学会按用户生命周期阶段分析行为特征
- 掌握基于历史行为的简单预测方法
- 学会从多维度对比分析用户行为差异

### 3. 核心任务
1. 计算新用户的次日留存、7日留存、30日留存率，绘制留存曲线
2. 按用户首次购买时间分组（Cohort），计算各Cohort的后续复购率，绘制Cohort留存矩阵热力图
3. 对比复购用户与一次性购买用户在以下维度的差异：购买品类分布、平均客单价、浏览深度、促销参与度
4. 分析用户的复购周期规律（如母婴类用户每XX天复购一次），输出各品类的典型复购周期
5. 基于用户历史行为特征（如过去30天购买次数、是否使用优惠券、平均浏览时长），构建简单的复购倾向评分模型

### 4. 数据说明
- **数据源**：项目一清洗后的用户订单数据 + 项目二的用户行为日志（可选）
- **关键字段**：user_id, order_date, amount, category, coupon_used, browse_duration

### 5. 技术要点
- Cohort分析（按首次购买月份分组，计算后续各月的复购率）
- 留存率计算：自定义聚合函数
- 热力图：`seaborn.heatmap()`
- 分组对比：`groupby().mean()` 等
- 简单评分模型：使用加权评分或逻辑回归（可选）

### 6. 任务步骤（参考）
1. 确定每个用户首次购买日期，标记为新用户获取时间
2. 对于每个用户，计算其在注册后第1天、第7天、第30天是否再次购买，得出留存率
3. 按月份进行Cohort分析：建立矩阵，行=首次购买月份，列=注册后第n个月，值=该月复购用户数/该cohort总用户数
4. 将用户分为“复购用户”（购买≥2次）和“一次性用户”，对比两者的平均客单价、品类偏好（饼图/柱状图）、促销参与率
5. 对每个品类，计算用户两次购买之间的平均间隔天数
6. 构建简单复购倾向评分：如分数 = 过去30天登录次数*0.3 + 收藏商品数*0.2 + 加购次数*0.5，按分数高低将用户分为高/中/低复购潜力

### 7. 拓展思考
- 如何排除促销活动对留存率的干扰？
- 如果用户第一次购买后很久才第二次购买，应如何定义“复购”？
- 留存分析如何指导产品功能优化？

---

## 项目七：用户画像构建与精准营销标签体系

### 1. 项目背景
市场部门需要深入了解不同用户群体的特征，建立可落地的用户画像标签体系，为精准营销提供数据支撑。项目需要从人口属性、消费行为、偏好特征等多个维度构建完整的用户画像。

### 2. 学习目标
- 掌握多源数据融合与特征工程方法
- 学会构建用户画像标签体系（基础属性、行为标签、价值标签、偏好标签）
- 掌握基于规则和基于统计的用户标签计算方法
- 学会用户画像的可视化呈现

### 3. 核心任务
1. 融合用户基础信息表（年龄、性别、地域）、行为日志表、订单表，构建用户级宽表
2. 构建多维用户标签：
   - 基础标签：年龄段（18-25/26-35/36-45/45+）、性别、城市等级
   - 行为标签：活跃时段（早/午/晚/夜）、登录频率（高频/中频/低频）
   - 价值标签：客单价分层（高/中/低）、生命周期阶段（新客/成长/成熟/流失）
   - 偏好标签：偏好品类Top3、价格敏感度（高/中/低）
3. 统计各标签维度的用户分布情况，绘制用户画像看板（如年龄分布饼图、品类偏好词云、地域分布地图）
4. 针对“高价值-母婴品类偏好”这一特定用户群体，输出完整的群体画像分析报告
5. 基于用户画像，为营销团队设计3个精准营销场景

### 4. 数据说明
- **数据源**：整合项目一至六的数据，并补充用户基础信息表（可模拟生成）
- **关键字段**：user_id, age, gender, city, registration_date, last_login, total_amount, favorite_category, price_sensitivity等

### 5. 技术要点
- 多表合并：`pd.merge()`
- 标签分箱：`pd.cut()`
- 聚合统计：`groupby().agg()`
- 可视化：饼图、柱状图、地图（如`pyecharts`或`plotly`）
- 词云：`wordcloud`库

### 6. 任务步骤（参考）
1. 读取用户信息表、订单表、行为日志表，通过user_id进行左连接，得到用户级宽表
2. 定义标签函数：
   - 年龄段：`pd.cut(age, bins=[0,18,26,36,45,100], labels=['<18','18-25','26-35','36-45','45+'])`
   - 活跃时段：根据用户行为日志中最密集的小时，划分早(5-11)、午(11-14)、晚(17-22)、夜(22-5)
   - 客单价分层：按用户平均订单金额的分位数划分
   - 偏好品类：按用户购买金额最多的品类
3. 统计各标签人数占比，绘制图表
4. 筛选条件：value_level='高' 且 favorite_category='母婴'，输出该群体的人数、平均客单价、主要地域、活跃时段等
5. 设计三个营销场景示例：
   - 向价格敏感型用户推送优惠券
   - 向夜猫子型用户推送深夜限时秒杀
   - 向母婴偏好用户推送亲子活动或新品

### 7. 拓展思考
- 用户标签的时效性如何管理？是否需要衰减？
- 如何处理跨品类偏好的用户（多个偏好）？
- 如何用机器学习自动生成标签（如聚类标签）？

---

## 项目八：电商用户聚类分析与客户分群

### 1. 项目背景
除了基于规则的RFM分层外，数据分析团队希望使用无监督学习算法——K-Means聚类——对用户进行更精细、更科学的群体划分，发现那些通过规则难以识别的隐藏用户模式。

### 2. 学习目标
- 掌握K-Means聚类算法的基本原理与实现
- 学会数据标准化/归一化（`StandardScaler`）
- 掌握肘方法（Elbow Method）确定最佳聚类数
- 学会聚类结果的解读与可视化
- 掌握聚类分析结果向业务策略的转化

### 3. 核心任务
1. 提取每个用户的聚类特征（如消费金额、购买频次、最近购买天数、平均客单价、平均浏览时长等），构造用户特征矩阵
2. 使用`StandardScaler`对特征进行标准化处理，消除量纲影响
3. 使用肘方法绘制SSE随K值变化的曲线，确定最佳聚类数（建议K=3~5）
4. 使用`KMeans`对用户进行聚类分群，将聚类标签写回用户表
5. 对每个聚类群体的特征均值进行分析，为每个群体命名（如“高消费活跃型”“低频高价型”“高频低价型”“沉睡型”等）
6. 可视化聚类结果（如PCA降维后的散点图、各群体特征雷达图）
7. 输出客户分群分析报告，为每个群体设计针对性的运营策略

### 4. 数据说明
- **数据源**：基于项目七构建的用户级宽表
- **关键特征**：总消费金额、购买次数、最近购买天数、平均客单价、平均浏览时长、优惠券使用次数、收藏商品数

### 5. 技术要点
- `sklearn.cluster.KMeans`
- `sklearn.preprocessing.StandardScaler`
- 肘方法：计算不同K值的`inertia_`，绘制曲线
- `sklearn.decomposition.PCA` 降维可视化
- 雷达图：`matplotlib` 或 `plotly`

### 6. 任务步骤（参考）
1. 选取聚类特征，删除缺失值（或填充）
2. 标准化特征矩阵
3. 对K=2到10，分别训练K-Means，记录SSE，绘制肘部图，选择拐点K
4. 使用选定的K值（如4）进行聚类，将`labels_`赋值给新列`cluster`
5. 按`cluster`分组，计算各特征的均值，观察不同群体的特征差异
6. 根据特征均值命名：
   - Cluster0: 高金额、高频率、最近购买天数小 → “忠诚活跃型”
   - Cluster1: 低金额、低频、最近购买天数大 → “沉睡流失型”
   - Cluster2: 高客单价、低频、浏览时长长 → “精挑细选型”
   - Cluster3: 低客单价、高频、优惠券使用多 → “薅羊毛型”
7. 使用PCA将特征降维到2维，绘制散点图，不同颜色表示不同簇
8. 绘制雷达图，展示各簇在原始特征上的对比
9. 为每个群体输出运营策略

### 7. 拓展思考
- K-Means对初始质心敏感，如何解决？（使用K-Means++，sklearn默认已采用）
- 如果特征中包含分类变量，应如何处理？（使用独热编码或改用K-Prototypes算法）
- 如何评估聚类效果（轮廓系数、Calinski-Harabasz指数）？

---

## 项目九：时间序列分析与销售预测

### 1. 项目背景
电商平台需要提前预测未来一周或一个月的销售量，为库存管理、仓储调配和营销活动规划提供决策依据。该项目将带领学生使用时间序列分析方法，探索数据的周期性规律并进行预测。

### 2. 学习目标
- 掌握时间序列数据的预处理与可视化
- 学会使用移动平均、指数平滑等方法进行趋势分解
- 理解自相关性（ACF/PACF）的概念
- 掌握简单的预测模型（如Holt-Winters、ARIMA或Prophet）
- 学会评估预测效果（MAE、RMSE）

### 3. 核心任务
1. 按日聚合历史销售数据，形成时间序列，绘制时间序列图观察整体趋势和周期性（如双十一前后的销售峰值）
2. 分解时间序列的三大成分：趋势（Trend）、季节性（Seasonality）、残差（Residual）
3. 使用Holt-Winters指数平滑模型或Prophet对时间序列进行建模预测
4. 将数据集分为训练集和测试集，使用训练集建模后在测试集上验证预测准确性
5. 输出未来7天/30天的销售预测值，并计算预测误差
6. 识别销售异常的日期（如销量远高于/低于预测区间），分析其背后的可能原因

### 4. 数据说明
- **数据源**：项目一清洗后的销售数据，按日期聚合
- **关键字段**：date, daily_sales_amount, daily_order_count

### 5. 技术要点
- `pd.date_range()`, `resample('D').sum()`
- `statsmodels.tsa.seasonal.seasonal_decompose`
- `statsmodels.tsa.holtwinters.ExponentialSmoothing`
- 或 `prophet` (Facebook Prophet)
- ACF/PACF图：`statsmodels.graphics.tsaplots.plot_acf`
- 评估指标：`mean_absolute_error`, `mean_squared_error`

### 6. 任务步骤（参考）
1. 按天聚合销售额，确保日期连续（缺失日期填充0）
2. 绘制时间序列图，观察是否有明显的趋势和周期性（如周周期、年周期）
3. 使用`seasonal_decompose(model='additive')`分解，绘制四张子图
4. 划分训练集（前80%）和测试集（后20%）
5. 训练Holt-Winters模型：`ExponentialSmoothing(train, trend='add', seasonal='add', seasonal_periods=7).fit()`
6. 预测测试集长度，计算MAE和RMSE
7. 使用模型预测未来7天，输出预测值和置信区间
8. 找出实际销量与预测值偏差超过2倍标准差的日期，结合日历分析原因（如促销、节假日）

### 7. 拓展思考
- 如果数据同时存在周周期和年周期，如何处理？
- 相比简单移动平均，指数平滑的优势在哪里？
- 如何将外部因素（如天气、促销活动）纳入预测模型？

---

## 项目十：AI增强的电商用户价值分析系统（综合实战）

### 1. 项目背景
本综合实战项目要求你整合前9个项目所学的全部技能，构建一个完整的电商用户价值分析系统。同时引入AI大模型辅助，展示如何利用AI技术增强传统数据分析的效率和深度，充分体现AI时代数据分析的新范式。该项目可作为课程大作业。

### 2. 学习目标
- 整合数据清洗、特征工程、数据分析、机器学习建模的全流程能力
- 掌握如何将AI大模型（如通过API调用的LLM）引入数据分析工作流
- 学会编写完整的数据分析报告与可执行的分析脚本
- 培养从业务问题到技术方案再到策略输出的端到端数据分析思维

### 3. 核心任务

#### 3.1 数据整合与清洗
融合用户信息表、行为日志表、订单表和商品信息表，构建完整的数据仓库宽表，完成全流程数据清洗与质量校验。

#### 3.2 多维度分析
- 基于项目四的购物篮分析，输出商品关联规则及捆绑销售策略
- 基于项目五的RFM模型，输出用户价值分层结果
- 基于项目八的聚类分析，输出客户分群结果及群体画像
- 基于项目九的时间序列预测，输出下一周期的销售预测

#### 3.3 AI增强环节（特色亮点）
- 使用大模型API（如OpenAI API、国内DeepSeek/文心一言等）对聚类得到的每个用户群体生成“群体特征描述与运营策略建议”的摘要文本
- 利用大模型对关联规则挖掘结果进行语义化解读（如“为什么购买笔记本的用户经常一起购买鼠标垫？”）
- 可选：使用大模型辅助生成数据分析报告的初稿和可视化图表的标题/说明文本

#### 3.4 综合报告输出
生成一份完整的《电商用户价值分析报告》，包含：
- 数据概况与清洗说明
- 用户分群画像（含聚类和RFM）
- 关联规则发现及解读
- 销售趋势预测
- AI增强分析洞察
- 运营策略建议

#### 3.5 代码与文档
提交完整的Jupyter Notebook代码、README文档和项目说明PPT（可选）。

### 4. 数据说明
- **数据源**：建议使用Kaggle上的公开电商数据集，如“Online Retail Dataset”或“Brazilian E-Commerce Dataset”（需包含订单、用户、商品、支付等多表）
- 如果公开数据缺少某些字段（如用户画像），可以合理模拟补充

### 5. 技术要点
- 前9个项目的全部技能
- LLM API调用（`requests`库或官方SDK）
- Prompt工程：设计有效的提示词以获取高质量的分析文本
- 综合报告撰写：Markdown + 图片嵌入，或生成PDF
- 项目文档整理：Git仓库管理、README编写

### 6. 任务步骤（参考）
1. **数据准备**：下载数据集，使用pandas读取，进行数据清洗（缺失值、异常值、类型转换）
2. **购物篮分析**：对订单明细做Apriori，输出Top10关联规则
3. **RFM分层**：计算R/F/M，打分，分群，统计各层特征
4. **聚类分析**：选择特征，标准化，肘方法选K，聚类，解读群体
5. **时间序列预测**：按日聚合销量，使用Prophet或Holt-Winters预测未来30天
6. **AI增强**：
   - 将聚类结果（各群体特征均值表）作为上下文，调用大模型API，要求：“请根据以下用户群体特征数据，为每个群体生成一段200字以内的描述，并给出3条运营建议。”
   - 将关联规则结果（前10条）发给大模型：“请用通俗易懂的语言解释以下关联规则，并说明可能的业务原因。”
   - 将生成的文本整合到报告中
7. **报告撰写**：使用Jupyter Notebook的Markdown单元格撰写报告，插入图表和AI生成的文本
8. **代码整理**：确保Notebook可从头到尾运行，添加必要的注释
9. **提交**：将Notebook导出为HTML或PDF，连同数据样例、README一起打包

### 7. 拓展思考
- 如何设计Prompt使得AI生成的策略更接地气、更具可执行性？
- 使用AI时如何保护数据隐私（不发送敏感数据）？是否可以本地部署小模型？
- 除了生成文本，AI还可以在数据分析中扮演什么角色（如自动选择模型、解释统计结果）？

### 8. 评分标准（参考）
- 数据清洗与预处理（10%）
- 四项核心分析完整度（40%，每项10%）
- AI增强环节的创意与实现（20%）
- 报告质量与可读性（20%）
- 代码规范与文档（10%）